version: '3'
services:
  sync:
    profiles:
      - dummy

  spec-generator:
    image: local/bus-documentation-test
    build:
      context: ../..
      dockerfile: contribs/Dockerfile
    volumes:
      - ../../contribs/documentation.py:/app/documentation.py:ro
      - ../../xivo_bus:/app/xivo_bus:ro

  spec-validator:
    image: asyncapi/cli

  rabbitmq:
    profiles:
      - dummy

  bus:
    profiles:
      - dummy
